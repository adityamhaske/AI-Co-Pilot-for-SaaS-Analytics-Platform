# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
# `.env.*` matters as much as `.env`: a file like `.env.demo` or `.env.local` holds a
# real secret and was previously not covered.
.env
.env.*
!.env.example
.venv
env/
venv/
ENV/

# Unit test / coverage reports
.pytest_cache/
.coverage
htmlcov/

# SQLite databases
*.db
*.db-journal

# Node
node_modules/
dist/
.eslintcache
.npm

# OS files
.DS_Store
.DS_Store?
._*
Thumbs.db

# Benchmark results (machine-specific, not meaningful to commit)
benchmark_results.json
